# Игра "Whack-a-Goblin" (Часть 1)

![CI](https://github.com/Ekaterina-siryk/game-goblin/actions/workflows/deploy.yml/badge.svg)

Первая часть игры "Бей гоблинов" (Whack-a-Mole), реализованная на чистом JavaScript внутри дерева DOM.

## Задание
Вы решили развлечься и реализовать некое подобие игры, где гномы (или другие существа), выскакивают из "отверстий" и по ним нужно бить молотком:

Авторские права принадлежат gfycat.com

Я
Соберите инфраструктуру проекта на базе Webpack, ESLint, Babel, Jest, Webpack Dev Server.

Поскольку мы более гуманны, вам необходимо реализовать лишь первую часть этой игры — перемещение объекта в дерево DOM.

Для этого самостоятельно разработайте игровое поле 4x4 и персонажа в виде картинки img(при включении страница должна программно ограничиваться и ставиться в произвольную позицию внутри игрового поля). С помощью функции setIntervalзапланируйте перемещение существующего объекта imgв другое поле (алгоритм - случайное перемещение, без перемещения в то же самое поле).

Для картинок персонажей используйте следующее:


Важно: не викоризировать removeChild! Проверьте, что будет, если Nodeизменить родителя.

Всё должно собираться через Webpack (включая картинки и стили) и выкладываться на Github Pages через CI.

В результате пришли к проверяющей ссылке на ваш GitHub-проект. Не забудьте установить бейджик сборки.


##  Ссылки
* **Демо-страница:** [https://ekaterina-siryk.github.io/game-goblin/](https://ekaterina-siryk.github.io/game-goblin/)
* **Репозиторий:** [https://github.com/Ekaterina-siryk/game-goblin](https://github.com/Ekaterina-siryk/game-goblin)

##  Особенности реализации
* Использован чистый JavaScript (ООП-подход с использованием классов) и сборка проекта на базе **Webpack 5** (стили, разметка и изображения импортируются через модули).
* **Важное условие:** Перемещение реализовано без использования метода `removeChild()`. Логика построена на автоматическом изменении родителя DOM-ноды при помощи метода `appendChild()`.
* Проверка качества и форматирования кода настроена через **ESLint** (конфигурация AirBnB).
* Реализованы автоматические DOM-тесты для проверки генерации игрового поля на базе **Jest** и среды **JSDOM**.
* Настроена автоматическая сборка и публикация проекта на GitHub Pages через **GitHub Actions** при каждом коммите в ветку `master`.

##  Команды проекта
* `npm install` — установка всех необходимых зависимостей
* `npm start` — локальный запуск проекта и сервера разработки (в памяти)
* `npm test` — запуск автоматических юнит-тестов Jest
* `npm run lint` — проверка качества кода линтером ESLint
* `npm run build` — продакшн сборка проекта в оптимизированную папку `dist`
